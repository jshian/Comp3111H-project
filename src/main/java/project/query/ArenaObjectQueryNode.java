package project.query;

class ArenaObjectQueryNode extends BinaryNode<ArenaObjectQuery> {

    /**
     * Constructs a newly allocated ArenaObjectQueryNode object.
     * @param data The data to store in the node.
     */
    ArenaObjectQueryNode(ArenaObjectQuery data) {
        super(data);
    }
}