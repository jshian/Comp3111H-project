package project.query;

class BinaryOperatorNode extends BinaryNode<BinaryOperator> {

    /**
     * Constructs a newly allocated BinaryOperatorNode object.
     * @param data The data to store in the node.
     */
    BinaryOperatorNode(BinaryOperator data) {
        super(data);
    }
}